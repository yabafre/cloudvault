# Story: 1-4-prisma-schema-folder-refactor — Upgrade Prisma to 7.7.0 and adopt the schema folder layout

**Epic:** 1 — Platform Foundation & Contract Layer
**Status:** ready-for-dev
**Ticket:** [KON-85](https://linear.app/koni/issue/KON-85)
**Branch:** `feature/KON-85-1-4-prisma-schema-folder-refactor`
**Size:** M (3 pts) — re-estimated from ticket's S (1 pt) due to Prisma 6 → 7 upgrade scope

## User Story

**As a** platform engineer, **I want** Prisma upgraded to 7.7.0 and the schema split into one `.prisma` file per model under `apps/api/prisma/schema/`, **so that** the codebase matches the architecture (§2.2 Data Layer), models stay readable as the domain grows, and we adopt the Rust-free client + driver-adapter pattern that is mandatory from v7.

## Acceptance Criteria

**AC1 — Prisma 7 dependencies installed**
- **Given** `apps/api/package.json`
- **When** inspected after `pnpm install`
- **Then** `prisma` and `@prisma/client` resolve to `^7.7.0`, `@prisma/adapter-pg` and `pg` are present as runtime dependencies, `@types/pg` is a devDependency, and `pnpm install` completes without peer-dependency warnings introduced by the upgrade.

**AC2 — Schema folder layout replaces the monolith**
- **Given** the folder `apps/api/prisma/schema/`
- **When** listed
- **Then** it contains exactly `schema.prisma` (datasource + generator), `user.prisma`, `file.prisma`, `refresh-token.prisma` — and the old monolithic `apps/api/prisma/schema.prisma` no longer exists. The generator block declares `provider = "prisma-client"` with `output = "../../src/prisma/generated"` and **no** `previewFeatures` (folder layout is GA in v7).

**AC3 — Client is generated under `src/prisma/generated/` and gitignored**
- **Given** the new schema
- **When** `pnpm db:generate` runs
- **Then** it produces `apps/api/src/prisma/generated/` containing the emitted client, and `apps/api/.gitignore` excludes that path so the artifact is never committed.

**AC4 — Zero-drift refactor on a fresh Neon database**
- **Given** the Neon database pointed to by `DATABASE_URL` reset to a clean state
- **When** `pnpm db:migrate` (which runs `prisma migrate dev`) is executed
- **Then** the existing `20260104040344_init` migration applies cleanly, no new migration is generated by the refactor, and `pnpm --filter @cloudvault/api exec prisma migrate status` reports the database in sync.

**AC5 — `PrismaService` works with the driver adapter**
- **Given** `apps/api/src/prisma/prisma.service.ts`
- **When** the Nest app boots
- **Then** `PrismaService` extends the new `PrismaClient` (imported from `@/prisma/generated/client`), instantiates `PrismaPg({ connectionString: process.env.DATABASE_URL })`, passes it via `super({ adapter })`, preserves `OnModuleInit`/`OnModuleDestroy` lifecycle hooks, and `cleanDatabase()` still deletes `file` then `user` in the correct order guarded by the NODE_ENV check.

**AC6 — All consumers updated, no `@prisma/client` imports remain**
- **Given** the repo
- **When** searched
- **Then** no file under `apps/api/src/**` imports from `@prisma/client` — every consumer (including `apps/api/src/modules/auth/auth.service.ts` importing `AuthProvider`) resolves to `@/prisma/generated/client`.

**AC7 — Monorepo stays green**
- **Given** all modifications
- **When** `pnpm --filter @cloudvault/api exec tsc --noEmit`, `pnpm build`, and `pnpm --filter @cloudvault/api test` run
- **Then** all three exit `0`, and `prisma.service.spec.ts` still passes against the refactored service.

**AC8 — Runtime smoke against Neon**
- **Given** `pnpm dev:api` started with the current `.env`
- **When** the service executes `prisma.$queryRaw\`SELECT 1 AS ok\``
- **Then** it returns `[{ ok: 1 }]`, confirming the adapter-based connection is live against the Neon pooler.

## Tasks

- [ ] Bump `apps/api/package.json`: `prisma` → `^7.7.0`, `@prisma/client` → `^7.7.0`; add runtime deps `@prisma/adapter-pg` (^7.7.0) and `pg` (^8.13.0); add devDependency `@types/pg` (^8.11.0); run `pnpm install` [AC1]
- [ ] Create `apps/api/prisma/schema/schema.prisma` with the datasource block (`provider = "postgresql"`, `url = env("DATABASE_URL")`) and the generator block (`provider = "prisma-client"`, `output = "../../src/prisma/generated"`) — no preview flags [AC2]
- [ ] Create `apps/api/prisma/schema/user.prisma` with the `User` model (unchanged columns, indexes, `@@map("users")`) and the `AuthProvider` enum [AC2]
- [ ] Create `apps/api/prisma/schema/file.prisma` with the `File` model (unchanged columns, indexes, `@@map("files")`) and the `FileStatus` enum [AC2]
- [ ] Create `apps/api/prisma/schema/refresh-token.prisma` with the `RefreshToken` model (unchanged columns, indexes, `@@map("refresh_tokens")`) [AC2]
- [ ] Delete `apps/api/prisma/schema.prisma` [AC2]
- [ ] Add `src/prisma/generated/` to `apps/api/.gitignore` and run `pnpm db:generate` — confirm the client lands at `apps/api/src/prisma/generated/client.ts` (or equivalent entry point) [AC3]
- [ ] Refactor `apps/api/src/prisma/prisma.service.ts`: import `PrismaClient` from `@/prisma/generated/client`, import `PrismaPg` from `@prisma/adapter-pg`, create the adapter in the constructor, call `super({ adapter, log: ... })`, preserve `onModuleInit`/`onModuleDestroy`/`cleanDatabase` verbatim [AC5]
- [ ] Update `apps/api/src/modules/auth/auth.service.ts:13` to import `AuthProvider` from `@/prisma/generated/client` [AC6]
- [ ] Grep `@prisma/client` across `apps/api/src/**` to confirm zero remaining imports (pnpm-lock.yaml entries are expected and fine) [AC6]
- [ ] Run `pnpm db:migrate` against the Neon DB reset to fresh, then `pnpm --filter @cloudvault/api exec prisma migrate status` — confirm no new migration was generated [AC4]
- [ ] Run `pnpm --filter @cloudvault/api exec tsc --noEmit`, `pnpm build`, `pnpm --filter @cloudvault/api test` — all green [AC7]
- [ ] Run `pnpm dev:api`, execute a one-off `SELECT 1` via a temp script or an existing seam (`prisma.service.spec.ts` integration point), confirm the adapter-based connection succeeds [AC8]

## Dev Notes

### Architecture (LAW)

- **Data Layer §2.2** — One `.prisma` file per model in `apps/api/prisma/schema/`, kebab-case names (`refresh-token.prisma`). Layout matches the architecture diagram verbatim.
- **Prisma version** — 7.7.0 per architecture table. In v7 the folder layout is GA, so no `previewFeatures = ["prismaSchemaFolder"]` is required.
- **No schema behavior change** — This story is a pure refactor + tooling upgrade. No column added, renamed, or dropped. No index altered. `@@map` and `@map` directives preserved so the database schema stays byte-identical.
- **Backend layer boundaries §3** — `PrismaService` stays the only place instantiating `PrismaClient`. Consumers keep importing it via Nest DI, never calling the client directly.

### Prisma 6 → 7 breaking changes that apply here

1. **Generator renamed**: `prisma-client-js` is deprecated; `prisma-client` (Rust-free, TS-only) is the v7 default.
2. **`output` is mandatory**: no more default emission to `node_modules/.prisma/client`. We target `apps/api/src/prisma/generated/` so the emitted code ships with the NestJS build and is reachable via the `@/prisma/*` path alias.
3. **Driver adapter is required**: `new PrismaClient()` without an adapter throws at runtime in v7. We use `@prisma/adapter-pg` since we run against Postgres (Neon). No change to `DATABASE_URL` — the adapter accepts the same pooler connection string (`channel_binding=require&sslmode=require`) as-is.
4. **Import paths change**: `@prisma/client` → `@/prisma/generated/client`. The existing TS path alias `@/*` → `src/*` (see `apps/api/tsconfig.json`) makes this clean across the codebase.
5. **`Prisma.validator` removed**: not used anywhere in the repo (verified by grep). Nothing to migrate.

### Schema folder content mapping (zero behavior delta)

Source file `apps/api/prisma/schema.prisma` (current monolith) splits as follows — each block copied verbatim, comments preserved:

- `schema.prisma` (new) ← lines 1–11 (datasource + generator), plus `output` and `provider = "prisma-client"`
- `user.prisma` ← `AuthProvider` enum (lines 13–17) + `User` model (lines 29–49)
- `file.prisma` ← `FileStatus` enum (lines 19–27) + `File` model (lines 67–90)
- `refresh-token.prisma` ← `RefreshToken` model (lines 51–65)

Cross-file relations (`User.files`, `User.refreshTokens`, `File.user`, `RefreshToken.user`) stay valid — Prisma merges every `.prisma` file in the schema folder into one logical schema at generate time.

### `PrismaService` refactor — target shape

```ts
import { Injectable, Logger, OnModuleDestroy, OnModuleInit } from '@nestjs/common';
import { PrismaPg } from '@prisma/adapter-pg';
import { PrismaClient } from '@/prisma/generated/client';

@Injectable()
export class PrismaService extends PrismaClient implements OnModuleInit, OnModuleDestroy {
  private readonly logger = new Logger(PrismaService.name);

  constructor() {
    const adapter = new PrismaPg({ connectionString: process.env.DATABASE_URL as string });
    super({
      adapter,
      log: process.env.NODE_ENV === 'development'
        ? ['query', 'info', 'warn', 'error']
        : ['error'],
    });
  }

  async onModuleInit() { /* unchanged */ }
  async onModuleDestroy() { /* unchanged */ }
  async cleanDatabase() { /* unchanged — still deletes file then user */ }
}
```

### `apps/api/.gitignore` diff

Append:
```
# Prisma 7 generated client
src/prisma/generated/
```

### Neon connection sanity

The current `DATABASE_URL` is the Neon pooled endpoint (`ep-rapid-credit-alonuddz-pooler.c-3.eu-central-1.aws.neon.tech`). `@prisma/adapter-pg` drives `pg`, which natively supports Neon's pooler with `channel_binding=require&sslmode=require`. No special TLS configuration is required.

### Commit workflow

- Commit prefix: `refactor(KON-85): ...`
- Magic words: `Part of KON-85` in intermediate commits, `Fixes KON-85` in the final commit.
- Single PR against `main` with title `refactor(KON-85): Story 1-4 - Upgrade Prisma to 7.7.0 and adopt schema folder layout`.

### Testing

- Existing `prisma.service.spec.ts` must continue to pass after the refactor. If it mocks `PrismaClient`, the mock target path changes from `@prisma/client` to `@/prisma/generated/client`.
- No new unit tests are introduced by this story (pure refactor), but the adapter-based boot path is covered by AC8 runtime smoke.
- Integration tests with Testcontainers are out of scope — introduced in a later story.

### Dependencies to install

Runtime (apps/api):
- `prisma` ^7.7.0 (already a devDependency — bump)
- `@prisma/client` ^7.7.0 (bump)
- `@prisma/adapter-pg` ^7.7.0 (new)
- `pg` ^8.13.0 (new)

Dev (apps/api):
- `@types/pg` ^8.11.0 (new)

### Files to create / modify / delete

**Create:**
- `apps/api/prisma/schema/schema.prisma`
- `apps/api/prisma/schema/user.prisma`
- `apps/api/prisma/schema/file.prisma`
- `apps/api/prisma/schema/refresh-token.prisma`

**Modify:**
- `apps/api/package.json` — bump Prisma + add adapter + pg
- `apps/api/.gitignore` — ignore `src/prisma/generated/`
- `apps/api/src/prisma/prisma.service.ts` — driver adapter + new import path
- `apps/api/src/modules/auth/auth.service.ts` — import `AuthProvider` from new path
- `pnpm-lock.yaml` — regenerated by `pnpm install`

**Delete:**
- `apps/api/prisma/schema.prisma`

### Out of scope

- Adding `directUrl` for Neon direct connections (follow-up story).
- Any schema or column change.
- Upgrading other modules or packages beyond Prisma.
- Introducing Testcontainers integration tests.
- Migrating `prisma.service.spec.ts` off mocking toward a real DB (covered later).

## Dev Agent Record

- **Model:** {{model used}}
- **Started:** {{timestamp}}
- **Completed:** {{timestamp}}

### Debug Log

### Completion Notes

### File List
